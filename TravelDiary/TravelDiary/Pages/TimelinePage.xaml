<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelDiary.Pages.TimelinePage">

  <ListView  x:Name="listView" ItemsSource="{Binding BlogList}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout BackgroundColor="#eee" Orientation="Vertical">
            <Image Source="{Binding Image}" />
               <Label Text="{Binding Title}" HorizontalOptions="EndAndExpand" TextColor="#503026" />
            <RelativeLayout>
              <Label Text="{Binding Place}"  TextColor="#f35e20" HorizontalOptions="Start" />
              <Label Text="{Binding Rate}"  TextColor="#f35e20" HorizontalOptions="End" />
            </RelativeLayout>
            <Label Text="{Binding Description}" HorizontalOptions="EndAndExpand" TextColor="#503026" />
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>